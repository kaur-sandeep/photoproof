@extends('admin.layouts.master')
@section('title', 'Orders and Payments')
@section('content')
<div class="container-fluid"><h3>Orders & Payments</h3>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif
<div class="card"><div class="table-responsive"><table class="table"><thead><tr><th>Order</th><th>Organization</th><th>Item</th><th>Amount</th><th>Status</th><th>Payment</th><th>Action</th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ $order->organization->organization_name }}</td><td>{{ ucfirst($order->order_type) }}: {{ $order->subscriptionPlan?->name ?? $order->topupPlan?->name }}</td><td>₹{{ $order->amount }}</td><td>{{ $order->status }}</td><td>{{ $order->payment_status }}</td><td>@if($order->payment_status === 'pending' && in_array($order->status,['pending','processing']))<form method="post" action="{{ route('admin.billing.orders.approve',$order) }}">@csrf <input class="form-control form-control-sm mb-1" name="transaction_reference" placeholder="Payment reference"><input class="form-control form-control-sm mb-1" type="date" name="payment_date"><button class="btn btn-sm btn-success">Approve</button></form><form method="post" class="mt-1" action="{{ route('admin.billing.orders.cancel',$order) }}">@csrf<button class="btn btn-sm btn-outline-danger">Cancel</button></form>@endif</td></tr>@endforeach</tbody></table></div></div>{{ $orders->links() }}</div>
@endsection
