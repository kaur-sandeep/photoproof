@extends('admin.layouts.master')
@section('title', 'Renew Plan')
@section('content')
<div class="container-fluid"><h3>Renew Plan</h3><form method="post" action="{{ route('owner.renew.store') }}">@csrf <div class="card"><div class="card-body"><div class="mb-3"><span class="d-block mb-1">Billing Period <span class="text-danger">*</span></span><label><input type="radio" name="billing_cycle" value="monthly" checked required> Monthly</label><label class="ms-3"><input type="radio" name="billing_cycle" value="yearly"> Yearly</label></div><div class="mb-3"><span class="d-block mb-1">Select Plan <span class="text-danger">*</span></span>@foreach($plans as $plan)<label class="d-block border rounded p-3 mb-2"><input type="radio" name="subscription_plan_id" value="{{ $plan->id }}" required> <strong>{{ $plan->name }}</strong> — {{ $plan->monthly_photo_limit }} photos/month — ₹{{ $plan->monthly_price }} / month; ₹{{ $plan->yearly_price }} / year</label>@endforeach</div> @error('subscription_plan_id')<div class="text-danger">{{ $message }}</div>@enderror @error('billing_cycle')<div class="text-danger">{{ $message }}</div>@enderror <button class="btn btn-primary">Create Offline-Payment Order</button></div></div></form></div>
@endsection
